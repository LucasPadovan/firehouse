<div class="modal">
  <%= simple_form_for([:configs, @sco], remote: true, html: { data: {
      target: dom_id(@sco), error: 'modal', modal: true, replace: true } }) do |f| %>
    <div class="modal-header">
      <h3><%= t('view.scos.edit_title') %></h3>
    </div>
    <div class="modal-body">
      <%= render partial: 'form', locals: { f: f } %>
    </div>
    <div class="modal-footer">
      <%= yield :form_actions %>
      <%= f.submit t('label.save'), class: 'btn btn-primary',
                   disable_with: t('label.saving') %>
    </div>
  <% end %>
</div>
